# Connection string for PostgreSQL.
# For local development with the provided docker-compose.yml:
DATABASE_URL=postgresql://ako:ako_password@localhost:5432/ako

# Used in Phase 2 for signing JWT sessions (generate with: openssl rand -base64 32)
SESSION_SECRET=change_me_in_production
